name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test-and-quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pytest-cov ruff mypy bandit pip-audit build

      - name: Ruff lint
        run: ruff check .

      - name: Ruff format check
        run: ruff format --check .

      - name: Type check
        run: mypy kernels implementations

      - name: Security scan
        run: bandit -r kernels implementations -q

      - name: Dependency integrity
        run: python -m pip check

      - name: Run tests with coverage
        run: pytest --cov=kernels --cov=implementations --cov-report=term-missing --cov-fail-under=80

      - name: Smoke test
        run: bash scripts/smoke.sh

      - name: Build package smoke
        run: python -m build --sdist --wheel
