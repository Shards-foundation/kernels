"""Reference implementations shipped with KERNELS.

These modules back the documented smoke tests and provide optional concrete
implementations for integrations such as thread-safe permit storage and
SQLite-backed audit storage. They are intentionally separate from the stable
``kernels`` API surface.
"""
